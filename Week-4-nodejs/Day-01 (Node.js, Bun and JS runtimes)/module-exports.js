// const math = require('./math(Exports-in-file)'); better to use import
import * as math from './ math(Exports -in -file).js';

console.log(math.sum(5, 4));
console.log(math.mul(5, 4));
console.log(math.PI);
console.log(math.g);
